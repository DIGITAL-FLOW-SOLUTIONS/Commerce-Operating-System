/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Sokoa Marketing API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

/**
 * Where the lead was captured.
 */
export type NewLeadSource = (typeof NewLeadSource)[keyof typeof NewLeadSource];

export const NewLeadSource = {
  homepage_hero: "homepage_hero",
  tiktok_page: "tiktok_page",
  socials_page: "socials_page",
  build_page: "build_page",
  boutique_page: "boutique_page",
  pricing_page: "pricing_page",
  footer: "footer",
  navbar: "navbar",
} as const;

/**
 * What the user wants to do.
 */
export type NewLeadIntentType =
  (typeof NewLeadIntentType)[keyof typeof NewLeadIntentType];

export const NewLeadIntentType = {
  scrape_facebook: "scrape_facebook",
  scrape_instagram: "scrape_instagram",
  tiktok_live: "tiktok_live",
  explore: "explore",
  signup: "signup",
  contact: "contact",
} as const;

export interface NewLead {
  /**
   * The actual user input (FB/IG link, business name, TikTok handle, email).
   * @minLength 1
   * @maxLength 500
   */
  inputValue: string;
  /** Where the lead was captured. */
  source: NewLeadSource;
  /** What the user wants to do. */
  intentType: NewLeadIntentType;
  /**
   * ISO country code (optional).
   * @maxLength 2
   */
  country?: string;
}

export interface Lead {
  id: string;
  inputValue: string;
  source: string;
  intentType: string;
  country?: string | null;
  /** Suggested next route in the funnel. */
  nextStep: string;
  /** Opaque session token to attach this lead to a future signup. */
  sessionToken: string;
  createdAt: string;
}

export interface RecentLead {
  id: string;
  /** Anonymized merchant name (e.g. "Aisha M."). */
  displayName: string;
  city: string;
  intentType: string;
  createdAt: string;
}

export interface PlatformStats {
  activeMerchants: number;
  storesCreatedToday: number;
  countriesServed: number;
  /** Gross merchandise value processed (KES). */
  gmvProcessedKes: number;
  averageStoreSetupSeconds: number;
  payoutsToday: number;
}

export interface ShowcaseStore {
  id: string;
  name: string;
  /** URL handle (e.g. "amani-boutique"). */
  handle: string;
  category: string;
  city: string;
  tagline: string;
  /** A single emoji used as a visual marker (NOT for UI, used as a data hint). */
  coverEmoji: string;
  productCount: number;
  monthlyRevenueKes: number;
}

export type TestimonialPlatform =
  (typeof TestimonialPlatform)[keyof typeof TestimonialPlatform];

export const TestimonialPlatform = {
  facebook: "facebook",
  instagram: "instagram",
  tiktok: "tiktok",
  whatsapp: "whatsapp",
} as const;

export interface Testimonial {
  id: string;
  name: string;
  /** Merchant role/title (e.g. "Boutique owner, Nairobi"). */
  role: string;
  quote: string;
  platform: TestimonialPlatform;
  /**
   * @minimum 1
   * @maximum 5
   */
  rating: number;
}

export type PricingPlanCadence =
  (typeof PricingPlanCadence)[keyof typeof PricingPlanCadence];

export const PricingPlanCadence = {
  hour: "hour",
  month: "month",
  year: "year",
  free: "free",
} as const;

export interface PricingPlan {
  id: string;
  name: string;
  /** Price in KES (Kenyan Shillings). 0 means free. */
  priceKes: number;
  cadence: PricingPlanCadence;
  tagline: string;
  features: string[];
  popular: boolean;
  ctaLabel: string;
}
